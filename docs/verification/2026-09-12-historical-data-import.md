# 历史数据导入验证报告

验证日期：2026-09-12
验证范围：Football-Data 英超 E0、2023/24 赛季单文件。

## 离线验证

| 检查 | 结果 |
|---|---|
| 后端 pytest（Python 测试框架） | 83 passed |
| 前端 Vitest（测试框架） | 2 passed |
| ESLint（代码规范检查） | 通过 |
| TypeScript/Vite 生产构建 | 通过，1671 个模块完成转换 |
| `git diff --check` | 通过 |

## 数据来源与规范地址

旧的 www 地址当前返回 HTTP 302，并重定向到非 www 地址。通过响应头确认后，目录改用官方规范 URL：

`https://football-data.co.uk/mmz4281/2324/E0.csv`

该修改遵循 TDD（测试驱动开发）：目录测试先出现 2 项预期失败，修复后 catalog、downloader 和 service 的 29 项相关测试通过。下载器没有启用不受限制的跨站重定向。

## 真实文件

- 文件大小：172196 字节。
- SHA-256（安全散列校验值）：`b2e057b0ed959f198b0f63d2391c01239f3608e6de5db68edab3f88e04d07ff3`。
- 下载耗时：约 3.7 秒。
- 解析耗时：约 0.02 秒。
- 解析结果：380 场比赛，0 行跳过，1140 个市场。

下载、缓存和数据库均使用隔离工作树内的临时路径，没有使用项目默认 DuckDB（嵌入式分析数据库）。验证结束后已清理临时 CSV 与数据库，运行时数据未提交到 Git（版本控制）。

## 首次持久化结果

| 项目 | 数量 |
|---|---:|
| 赛事 | 1 |
| 球队 | 20 |
| 比赛 | 380 |
| 市场快照 | 1140 |
| 市场选项 | 2660 |
| 具有半场比分的比赛 | 380 |
| 具有小数亚洲盘口的快照 | 289 |
| `kickoff_bound` 时间违规 | 0 |

首次 DuckDB 持久化约 226.8 秒，运行状态为 `completed`，导入比赛数为 380。

## 重复导入验证

第二次运行使用已校验的本地缓存，没有再次请求网络。运行约 170.5 秒后返回：

- `status=completed`；
- `imported_matches=0`；
- 比赛、球队、市场快照和市场选项仍为 380、20、1140、2660。

这证明真实文件的重复导入不会增加业务数据，同时仍保留独立运行审计。

## 已知性能问题

正确性、时间语义和幂等性均验证通过，但单文件逐条 DuckDB 查询与写入约需 3–4 分钟。批量导入 38 个联赛、5 个赛季前，应增加批处理或导入期缓存优化；当前不应直接执行全部 190 个文件。
